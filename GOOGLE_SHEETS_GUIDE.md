# 📖 คู่มือการติดตั้งและใช้งาน Google Sheets เป็นฐานข้อมูล (Database Backend Guide)

ระบบลงคะแนน **MC OF ISKKU 2026** รองรับการใช้ **Google Sheets** เป็นฐานข้อมูลหลักหรือฐานข้อมูลสำรองพร้อมระบบ Real-time Sync ทุกเครื่องที่เข้าใช้งาน!

---

## 🌟 ฟีเจอร์ของระบบ Google Sheets Integration
1. **จัดเก็บข้อมูลครบทุกส่วน**:
   - `Users`: ข้อมูลกรรมการและผู้ดูแลระบบ
   - `Rounds`: ข้อมูลรอบการแข่งขันและการเปิด/ปิดรอบ
   - `Contestants`: รายชื่อและข้อมูลผู้เข้าแข่งขัน
   - `Criteria`: เกณฑ์การให้คะแนนและคะแนนเต็ม
   - `Pairs`: รายชื่อคู่จับแข่งขันประจำรอบ (Round 2, 3, 4)
   - `Scores`: ผลคะแนนรวมที่กรรมการแต่ละท่านลงคะแนน
   - `ScoreDetails`: รายละเอียดคะแนนย่อยประจำเกณฑ์
   - `AuditLogs`: ประวัติการดำเนินการของระบบ
   - `TieBreakerVotes`: การลงคะแนนโหวตกรณีคะแนนเสมอกัน
2. **ซิงค์ข้อมูล Real-Time อัตโนมัติ**: เมื่อกรรมการกดส่งคะแนน หรือ Admin แก้ไขข้อมูล ข้อมูลจะถูกส่งไปบันทึกลง Google Sheets ทันที
3. **อัปเดตตรงกันทุกอุปกรณ์**: หน้าจอของกรรมการและ Admin ทุกเครื่องจะดึงข้อมูลที่อัปเดตตรงกันเสมอ

---

## 🚀 ขั้นตอนการติดตั้งและเปิดใช้งาน (Setup Steps)

### ขั้นตอนที่ 1: สร้างไฟล์ Google Sheets ใหม่
1. ไปที่เว็บไซต์ [sheets.new](https://sheets.new) เพื่อสร้าง Google Sheets ใหม่
2. ตั้งชื่อไฟล์ตามต้องการ เช่น `MC_OF_ISKKU_2026_DATABASE`

### ขั้นตอนที่ 2: เปิด Apps Script และวางโค้ด Code.gs
1. ที่เมนูด้านบน ไปที่ **ส่วนขยาย (Extensions) > Apps Script**
2. ลบโค้ดเดิมในหน้าต่าง `Code.gs` ออกทั้งหมด
3. คัดลอกโค้ดทั้งหมดจากไฟล์ [`Code.gs`](file:///d:/SMOIS/student-club-web/%E0%B8%A3%E0%B8%B0%E0%B8%9A%E0%B8%9A%E0%B8%A5%E0%B8%87%E0%B8%84%E0%B8%B0%E0%B9%81%E0%B8%99%E0%B8%99/Code.gs) ในโปรเจกต์ มาวางใน Apps Script

### ขั้นตอนที่ 3: ทำให้ใช้งานเป็นเว็บแอป (Deploy as Web App)
1. กดปุ่ม **ทำให้ใช้งานได้ (Deploy)** บริเวณมุมขวาบน > เลือก **การเปิดตัวใช้งานใหม่ (New Deployment)**
2. คลิกไอคอนรูปเฟือง ⚙️ ข้าง "เลือกประเภท" > เลือก **เว็บแอป (Web App)**
3. กำหนดค่าการเปิดใช้งานดังนี้:
   - **คำอธิบาย (Description):** `MC OF ISKKU 2026 Database Backend`
   - **การเข้าถึงในฐานะ (Execute as):** `ฉัน (Me)` *(อีเมล Google ของคุณ)*
   - **ผู้มีสิทธิ์เข้าถึง (Who has access):** `ทุกคน (Anyone)` *(สำคัญมาก)*
4. กดปุ่ม **ทำให้ใช้งานได้ (Deploy)**
5. กดปุ่ม **ให้สิทธิ์การเข้าถึง (Grant Permissions)** > เลือกบัญชี Google ของคุณ > กด **Advanced** > กด **Go to Untitled project (unsafe)** > กด **Allow**
6. คัดลอก **URL เว็บแอป (Web App URL)** ที่ขึ้นต้นด้วย `https://script.google.com/macros/s/.../exec`

### ขั้นตอนที่ 4: เชื่อมต่อ Web App URL บนระบบ Admin
1. เปิดหน้า Admin บนเว็บไซต์ (http://localhost:8000)
2. ไปที่แท็บ **`⚙️ จัดการรอบการแข่งขัน`**
3. เลื่อนลงมาที่กล่อง **`📊 Google Sheets Real-Time Database Sync`**
4. วาง URL ที่คัดลอกมาลงในช่อง **Google Apps Script Web App URL**
5. กดปุ่ม **`💾 บันทึก URL`**
6. กดปุ่ม **`⚡ ซิงค์ข้อมูลทั้งหมดไป Google Sheets`**

---

🎉 **เสร็จสิ้น!** ตารางทั้งหมดใน Google Sheets จะถูกสร้างขึ้นและบันทึกข้อมูลแบบ Real-Time อัตโนมัติทันทีครับ!
